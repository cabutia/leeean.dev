---
const { class: className, as = "div", ...attrs } = Astro.props;

const Tag = as;
---

<Tag class={`container mx-auto flex flex-col gap-2 px-6 lg:px-8 pt-32 pb-12 ${className}`} {...attrs}>
    <slot />
</Tag>
